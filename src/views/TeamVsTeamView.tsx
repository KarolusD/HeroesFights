import React from 'react'
import MainTemplate from '../templates/MainTemplate'

interface Props {}

const TeamVsTeamView = (props: Props) => {
  return (
    <MainTemplate>
      <p>Team vs team</p>
    </MainTemplate>
  )
}

export default TeamVsTeamView
