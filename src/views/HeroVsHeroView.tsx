import React from 'react'
import MainTemplate from '../templates/MainTemplate'

interface Props {}

const HeroVsHeroView: React.FC = (props: Props) => {
  return (
    <MainTemplate>
      <p>Hero vs hero!</p>
    </MainTemplate>
  )
}

export default HeroVsHeroView
